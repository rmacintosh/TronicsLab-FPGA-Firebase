import { RawCommands } from '@tiptap/core';

declare module '@tiptap/core' {
  interface RawCommands {
    // This file is for global Tiptap command declarations.
    // Individual extensions should declare their own commands.
  }
}
