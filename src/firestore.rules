
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {

    // Articles can be read by anyone.
    // Creation is limited to admins and authors.
    // Updates and deletions are limited to admins or the article's original author.
    match /articles/{articleId} {
      allow get, list: if true;
      allow create: if 'admin' in request.auth.token.roles || 'author' in request.auth.token.roles;
      allow update, delete: if ('admin' in request.auth.token.roles) || (resource.data.authorId != null && request.auth.uid == resource.data.authorId);
    }

    // Categories can be read by anyone.
    match /categories/{categoryId} {
      allow get, list: if true;
      allow write: if 'admin' in request.auth.token.roles;
    }
    
    // Comments can be read by anyone, created by any authenticated user, but only managed by admins or moderators.
    match /comments/{commentId} {
      allow get, list: if true;
      allow create: if request.auth != null;
      allow update, delete: if 'admin' in request.auth.token.roles || 'moderator' in request.auth.token.roles;
    }

    // Rules for the 'users' collection
    match /users/{userId} {
      // Admins can list all users for the admin dashboard
      allow list: if 'admin' in request.auth.token.roles;
      // Any authenticated user can get a single user's document
      allow get: if request.auth != null;
      // A user can only write to their own document
      allow write: if request.auth.uid == userId;
    }

    // Rules for the 'images' collection
    match /images/{imageId} {
      // Allow reading of image documents by anyone, as they contain public URLs.
      allow get: if true;
      // Allow creation by authenticated authors/admins.
      allow create: if request.auth != null && ('author' in request.auth.token.roles || 'admin' in request.auth.token.roles);
      // Allow updates by the system (for processing) or by the image owner/admin.
      allow update: if request.auth != null && (resource.data.userId == request.auth.uid || 'admin' in request.auth.token.roles);
      // Allow deletion only by the owner or an admin.
      allow delete: if request.auth != null && (resource.data.userId == request.auth.uid || 'admin' in request.auth.token.roles);
    }

    // Settings can only be accessed by admins.
    match /settings/{docId} {
        allow read, write: if 'admin' in request.auth.token.roles;
    }
  }
}
